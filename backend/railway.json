{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "mkdir -p tmp/pids /data/storage && bundle exec puma -C config/puma.rb",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "releaseCommand": "bundle exec rails db:prepare && bundle exec rails db:seed || echo 'Seeds completed with warnings'"
  }
}

