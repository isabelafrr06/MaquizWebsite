{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bundle exec puma -C config/puma.rb",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "releaseCommand": "bundle exec rails db:create 2>/dev/null || true && bundle exec rails db:migrate && (bundle exec rails db:seed || echo 'Seeds completed with warnings - check logs')"
  }
}

